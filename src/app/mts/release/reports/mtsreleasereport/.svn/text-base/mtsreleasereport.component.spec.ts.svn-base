import { ComponentFixture, TestBed } from '@angular/core/testing';

import { MtsreleasereportComponent } from './mtsreleasereport.component';

describe('MtsreleasereportComponent', () => {
  let component: MtsreleasereportComponent;
  let fixture: ComponentFixture<MtsreleasereportComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ MtsreleasereportComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(MtsreleasereportComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
