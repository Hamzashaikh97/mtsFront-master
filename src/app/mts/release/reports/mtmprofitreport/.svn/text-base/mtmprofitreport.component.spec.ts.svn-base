import { ComponentFixture, TestBed } from '@angular/core/testing';

import { MTMprofitreportComponent } from './mtmprofitreport.component';

describe('MTMprofitreportComponent', () => {
  let component: MTMprofitreportComponent;
  let fixture: ComponentFixture<MTMprofitreportComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ MTMprofitreportComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(MTMprofitreportComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
