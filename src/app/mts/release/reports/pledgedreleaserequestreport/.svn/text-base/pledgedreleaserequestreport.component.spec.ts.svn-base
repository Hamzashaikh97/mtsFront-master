import { ComponentFixture, TestBed } from '@angular/core/testing';

import { PledgedreleaserequestreportComponent } from './pledgedreleaserequestreport.component';

describe('PledgedreleaserequestreportComponent', () => {
  let component: PledgedreleaserequestreportComponent;
  let fixture: ComponentFixture<PledgedreleaserequestreportComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ PledgedreleaserequestreportComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(PledgedreleaserequestreportComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
