import { ComponentFixture, TestBed } from '@angular/core/testing';

import { MtsunreleasereportComponent } from './mtsunreleasereport.component';

describe('MtsunreleasereportComponent', () => {
  let component: MtsunreleasereportComponent;
  let fixture: ComponentFixture<MtsunreleasereportComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ MtsunreleasereportComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(MtsunreleasereportComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
